name: Run tests
on:
  workflow_call:
    inputs:
      tests_folder:
        description: Api | UI
        required: true
        type: string
jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: OpenMRS Setup
      uses: ./.github/actions/openmrs-setup